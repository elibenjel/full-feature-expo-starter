import { Colors } from 'react-native-ui-lib'

/**
 * Define and customize the theme for the app
 */
Colors.loadDesignTokens({ primaryColor: '#d3af37' })
Colors.loadColors({
  screenBG: Colors.$backgroundDefault,
})
